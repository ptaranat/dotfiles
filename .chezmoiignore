README.md
Brewfile
.github/
*.zwc
*.bak
.vim/undo/

# Stock themes shipped and regenerated by superfile itself.
.config/superfile/theme/

{{ if ne .chezmoi.os "darwin" }}
# macOS-only. aerospace outright; ghostty is cross-platform but
# not the terminal on WSL, and keeping it would also clone its shader repo.
.config/aerospace
.config/ghostty

# Scripts match on target name, so the prefix and .tmpl suffix are stripped
# here. Naming the source file silently fails to match and it still runs.

# `defaults` does not exist on Linux, so this fails on every apply.
20-macos-defaults.sh

# Brewfile-driven; 11-packages-linux.sh is the apt half. Exactly one runs.
10-packages.sh
{{ end }}

{{ if eq .chezmoi.os "darwin" }}
# apt-driven, and the other half of the pair above.
11-packages-linux.sh
{{ end }}

{{ if not (and .work .workemail) }}
# Only a work machine has a work identity to commit as.
.config/git/work.gitconfig
{{ end }}
