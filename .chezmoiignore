README.md
Brewfile
.github/
*.zwc
.vim/undo/

{{ if ne .chezmoi.os "darwin" }}
# macOS-only. aerospace is a macOS window manager outright; ghostty and
# alacritty are cross-platform but are not the terminal on the WSL box, and
# leaving ghostty in would also make .chezmoiexternal clone its shader repo.
# Flip any of these back by removing the line if the tool gets installed.
.config/aerospace
.config/ghostty
.config/alacritty

# Scripts are matched on their target name, so the run_onchange_after_ prefix
# and any .tmpl suffix are stripped here. Naming the source file instead
# silently fails to match and the script still runs.

# Writes macOS system defaults. On Linux the `defaults` command does not exist,
# so this fails on every apply.
20-macos-defaults.sh

# Brewfile-driven. The Linux counterpart is 11-packages-linux.sh, which reads
# the apt lists in .chezmoidata.yaml; the two are a matched pair and exactly
# one of them runs per machine.
10-packages.sh
{{ end }}

{{ if eq .chezmoi.os "darwin" }}
# apt-driven, and the other half of the pair above.
11-packages-linux.sh
{{ end }}
